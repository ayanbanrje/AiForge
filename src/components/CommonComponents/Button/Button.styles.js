// src/components/commonComponents/Button/button.style.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#007BC0', // Default button background color
    padding: 10,
    alignItems: 'center',
  },
  buttonText: {
    color: '#FFFFFF', // Default text color
    fontSize: 16,
  },
});

export default styles;
